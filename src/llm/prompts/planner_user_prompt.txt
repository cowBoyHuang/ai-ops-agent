请基于以下信息生成 hypothesis、investigation_goals、required_answers，仅输出 JSON。

【用户问题】
{user_query}

【改写后查询】
{normalized_query}

【关键词】
{keywords}

{domain_knowledge}

{case_knowledge}

{code_knowledge}

【已证伪假设（不要重复）】
{rejected_hypothesis}

【上轮重规划原因】
{replan_reason}

请识别用户问题中的“必答字段诉求”（例如字段名、标识ID、错误编码、关键状态、时间点、返回值等），
并写入 required_answers，标记 required=true。不要限制为固定字段名集合。

请特别注意：
- required_answers 中如果有多个 required=true 字段，investigation_goals 必须拆分为多个最小执行单元。
- 每条 goal 只能对应一个字段或一个单一信息目标，不允许把“失败原因 + 错误码”等多个目标混在同一条里。
- required_answers 必须与“当前用户问题”严格对齐；只保留用户明确需要回答的字段，不要补充额外字段。
