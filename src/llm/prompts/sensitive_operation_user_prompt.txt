请判断以下用户输入是否安全。若包含危险命令执行、删库删盘、提权、破坏系统、绕过安全控制、批量导出敏感数据等敏感操作，请判定不通过。
输出严格 JSON，字段为：allow(boolean), reason(string)。
不需要解释文本，仅需要输出结构化数据，reason 最多 30 个字。
禁止输出推理过程、Markdown、代码块标签、<think>或</think>。
用户输入：{question}
