请基于以下上下文进行单轮 ReAct 决策，并返回严格 JSON：

【用户问题】
{question}

【执行计划（Planner 输出）】
{plan_json}

【必答字段（required_answers，必须关注）】
{required_answers_json}

【Planner 已读取文档（必须结合文档与 skill）】
{planner_knowledge_json}

【当前子任务】
{current_subtask_json}

【上一步观察结果（Observation）】
{previous_observation_json}

【可用工具定义（Function Schema）】
{tool_schemas_json}

【最近执行历史】
{history_preview}

【是否强制兜底 queryLog（true/false）】
{force_querylog}

【兜底说明】
{force_querylog_reason}

请严格遵循以下决策顺序（只用于你内部推理，不要输出额外字段）：
0. 先做“问题-目标对齐”：
   - 从用户问题中提取本轮要回答的核心对象与字段；
   - 校验与当前 goal 是否一致；若不一致，以当前 goal 为准并在 thought 中说明。
   - 本轮 action 必须直接服务该字段/对象，不要做与当前目标无关的泛化检索。
1. 从 required_answers 中识别“本 goal 仍缺失的必要字段”。
2. 从 planner_knowledge_json 中定位与该字段最相关的文档证据（字段定义、日志位置、系统边界）。
3. 从 tool_schemas_json 中选择最可能直接获得该字段的注册工具。
3.1 如果 goal 是“获取具体字段值”（例如年龄/错误码/姓名），优先日志查询类工具；文档检索工具仅用于补充线索，不可替代字段取值查询。
4. 若最近历史中同类调用已执行但字段仍缺失，必须切换 skill/方法/范围，不得重复相同调用。
4.1 若进入多轮 reactor（act>=2），禁止选择与历史“完全相同”的调用（tool_name、app_code/logname、match_phrase_list、match_list 全相同）。
    若要复用同一工具，必须给出参数变化后的新调用。
5. 若关键字段仍未获得，优先继续补证并保持 advance_plan_step=false；若多次重试后仍无法精确命中，可输出“相近字段+不确定性说明+下一步排查建议”，并允许推进。
6. 若本轮选择 queryLog：
   - 先从问题、goal、required_answers 缺失字段、上一轮 observation 中提取关键词；
   - match_phrase_list 只填 traceId 或 orderNo（或两者）；不要放业务关键词；
   - match_list 必须为 []（不要填任何词）；
   - 且必须补齐 app_code 与 logname（依据 skill 与文档自行判断，不允许留空）。
7. 当 force_querylog=true 时：
   - 先执行“问题-文档匹配”：根据用户问题与当前 goal，从 planner_knowledge_json 中选择最相关的 1-2 篇 RAG 文档（优先字段定义明确、系统边界明确的文档）。
   - 你选择的 app_code/logname 必须来自这 1-2 篇文档给出的业务语义与系统边界；不要忽略与用户问题直接相关的文档。
   - 若文档间有冲突，优先选择与“用户问题对象”最一致的文档（例如用户问机票子单，就优先机票子单链路文档，而不是总单聚合文档）。
   - 本轮 action 必须选择 `tool_name=queryLog`；
   - 必须生成完整参数：`app_code`、`logname`、`begin_time`、`end_time`、`match_phrase_list`、`match_list=[]`；
   - 不允许输出 `knowledge_lookup` 或 `rag_parent_chunk_query` 作为本轮 action。
8. thought 中必须明确写出“用户问题 -> goal -> 本轮工具调用”的映射关系，避免出现工具调用与目标脱节。
